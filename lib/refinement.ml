(* type ('elt, 'witness) s = {
 *   old : ('elt, 'witness) Set.t;
 *   new_ : ('elt, 'witness) Set.t;
 * }
 * [@@deriving compare, sexp]
 * 
 * type t = s Map.M(Search_state.Args).t [@@deriving sexp_of] *)
